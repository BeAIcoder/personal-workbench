# 个人工作台 · 使用说明

> 给日常使用者的操作手册。启动方式：双击项目里的 **启动工作台.bat**，浏览器会自动打开
> <http://127.0.0.1:8000>（建议按 Ctrl+D 收藏）。关闭服务：双击 **停止服务.bat**（会先校验进程并请你确认），或直接关闭后端黑色窗口。

## 0. 三种启动方式

| 方式 | 入口 | 适用场景 |
| --- | --- | --- |
| 一键启动 | `启动工作台.bat` | 日常使用：自动建虚拟环境、装依赖、启动服务 |
| 开发模式 | `开发模式.bat` | 改代码：后端热重载（8000）+ 前端 Vite 热更新（5173） |
| 手动 | 见 README「手动方式」 | 其它系统或排查问题 |

## 1. 工作台概览（首页）

- 顶部四张卡片：**待办任务、进行中任务、已逾期任务、今日日程**，点击卡片直接跳到对应列表。
- 中间两张图：任务状态饼图（含完成率）、近 7 天完成任务柱状图。
- 底部三栏：即将到期任务、今日日程、最近笔记，点击任意条目可跳转。

## 2. 任务管理

- **新建**：右上角「新建任务」→ 填标题（必填）、描述、状态、优先级、截止时间、分类 → 保存。
- **完成**：列表里点「完成」，任务标记为已完成并记录完成时间（首页趋势图随之更新）。
- **筛选**：顶部可按状态、优先级过滤；搜索框支持标题和描述关键词，回车生效。
- **排序**：可按创建时间 / 截止时间 / 优先级排序。
- **逾期提醒**：截止时间已过且未完成的任务，截止时间显示红色并带「已逾期」标签。
- **编辑 / 删除**：行内按钮；删除有二次确认，不可恢复。

## 3. 日程管理

- 左侧是**月历**，有日程的日期下方显示彩色圆点（颜色即日程颜色），最多显示 3 个，多的显示 +N。
- 点某一天，右侧列出该天的日程；点右上角「新建日程」默认建在当前选中日期。
- 新建/编辑：标题、日期时间（可勾选**全天**）、地点、颜色、备注。
- 切换月份时日程点自动刷新。

## 4. 笔记管理

- 左侧列表：顶部搜索框（搜标题和内容）+ 标签下拉筛选；置顶笔记带 ★ 且排在最前。
- 「新建」会立即生成一条「未命名笔记」并进入编辑。
- 右侧编辑器：标题、标签（输入后回车即可创建新标签）、正文多行内容。
- 修改后右上角出现「保存」按钮，保存后变灰「已保存」；未保存就切换笔记会提示确认。
- 「置顶」按钮一键置顶/取消；删除有二次确认。

## 5. 全局搜索

- 顶栏搜索框，快捷键 **Ctrl + K**。
- 输入关键词（如「发票」），下拉实时显示任务 / 日程 / 笔记三类结果（带类型标签和内容摘要）。
- 点击结果会跳到对应模块，并自动带入关键词筛选。

## 6. AI 助手（Agent 团队）

- 左侧菜单点 **AI 助手**，进入对话页。左栏是会话列表与专家名册，右栏是对话区（回复流式逐字输出，可随时点停止打断）。
- 默认「智能路由」：系统根据你的问题自动匹配最合适的专家（也可手动点选某位专家，对话只由该专家应答）。
- 三种工作模式可切换：**标准执行**（可查可写）/ **只读咨询**（只分析不改数据）/ **深度研究**（更多轮次的多步分析）。
- 可以直接自然语言操作工作台，例如：
  - 「帮我把明天上午 10 点与品牌方洽谈建个日程」
  - 「创建一条跟进任务：某品牌月底前确认租决条件，高优先级」
  - 「工作台现在有多少逾期任务？」
  - 「粘贴凭证分录，帮我核对借贷是否平衡」
- 支持**上传附件**（单文件 10MB 内；Excel 会自动转成 CSV 供分析）。
- 专家使用工作台数据后，回复下方可展开「🔧 工具调用」查看它实际执行了什么（可审计）。

### 6.1 首次配置模型

点 AI 助手页左栏「⚙ 设置」打开设置抽屉，在**模型**tab：

1. 新增一个供应商（可选预设：ModelScope / 硅基流动 / DeepSeek / 火山方舟 / DashScope 兼容 / Ollama，或自定义），填入 API Key；
2. 在供应商下添加模型（候选模型会自动带出），点「测试连通性」验证；
3. 把模型「设为激活」，助手立即可用，无需重启。

未配置时助手显示「模型未配置」，主工作台功能不受影响。也可以在 `backend/.env` 填 `LLM_BASE_URL/LLM_API_KEY/LLM_MODEL` 作为兜底。

### 6.2 专家团队怎么配（设置抽屉「专家团队」tab）

团队以卡片式管理，内置 7 位专家（网络安全/招商/运营/企划/工程物业/信息化财务/商业地产管家），全部可改：

- **新建专家**：填英文标识（小写字母/数字/下划线）、中文名、一句话职责、角色提示词、关键词。
- **关键词**：路由靠它命中——词越具体越准；命中词多的专家优先，同分则排序值（sort）小者优先。
- **启用开关**：停用后该专家不再参与路由，也不出现在名册。
- **绑定模型**：每位专家可单独选一个已配置的模型（如给财务专家配更强的模型），不选则跟随全局激活模型。
- **路由测试**：输入一句话预览会被哪位专家接住、命中了哪些词，用来调关键词。
- **恢复出厂**：一键把内置专家恢复为出厂定义（按标识覆盖/补插，不影响你自建的专家）。
- 所有改动保存后立即生效，不用重启服务。

### 6.3 从 QwenPaw 导入 Agent

如果本机装了 **QwenPaw**，可以把它的 Agent 角色定义批量导进专家团队，不用手抄提示词。在 `backend` 目录下执行：

```bash
venv\Scripts\python scripts\import_qwenpaw_agents.py --dry-run    # 预览会合并/新建什么，不写库
venv\Scripts\python scripts\import_qwenpaw_agents.py              # 正式导入
venv\Scripts\python scripts\import_qwenpaw_agents.py --overwrite  # 剥掉旧导入内容后重导
```

- **读什么**：每个 Agent 工作区里的角色定义文件（SOUL.md / PROFILE.md / AGENTS.md / 00_系统总则.md / 06_协同规则.md），按序拼接、脱敏后作为角色提示词。
- **自动脱敏**：「老头子」等称呼统一替换为「用户」，本机绝对路径替换为 `[本地路径]`，个人痕迹不会写进工作台。
- **覆盖合并规则**：领域重叠的合并进内置专家（cre-leasing→招商、cre-ops→运营、cre-mkt→企划、cre-fm→工程物业、cre-finance→信息化财务、cre-it→网络安全），追加角色定义并并入关键词；职责全新的新建专家：cre_gm 商业地产总经理、cloud_orchestrator CloudPaw 主控编排、cloud_executor CloudPaw 执行器、cloud_verifier CloudPaw 验证器、datapaw DataPaw 数据分析。
- **幂等**：导入的角色提示词带【QwenPaw 整合】标记，重复执行自动跳过已导入的专家，不会叠加。
- **生效方式**：导入后 60 秒内团队缓存自动刷新，重启服务则立即生效。导入只写本机数据库（agent_specs 表），不进 git 仓库。
- **工作区路径**：默认 `D:/AI_Service/QwenPaw/data/workspaces`，可用 `--from` 或环境变量 `QWENPAW_WORKSPACES` 指定；本机没装 QwenPaw 会友好退出，不影响其他功能。
- **不导入什么**：只导角色定义；QwenPaw 的记忆文件、会话记录等数据不导入。

### 6.4 高级参数（设置抽屉）

最大工具轮次（Agent 单轮最多调用几次工具）、记忆开关与注入历史条数（关闭则不携带上下文）、思考深度；「技能池」「允许 Bash」「插件」为实验性开关，默认关闭，知悉风险后再开。

## 7. 定时任务

左侧菜单点 **定时任务**（在 AI 助手之后，Timer 图标），进入任务列表页：到点自动执行，结果写成笔记回看。

- **新建任务**：右上角「新建任务」→ 填：
  - **名称**：任务的名字，同时作为结果笔记的标签之一；
  - **提示词**：到点时交给专家的问题/指令，写得越具体执行越稳；
  - **执行专家**：选某位专家固定执行，或选「智能路由」由系统按内容自动匹配；
  - **工作模式**：标准执行（可查可写）/ 只读咨询 / 深度研究，与 AI 助手一致；
  - **调度方式**二选一：**间隔**（每 N 分钟，最短 5 分钟、最长 7 天）或 **每天**（每天到达 HH:MM 执行）。
- **启停**：列表里每行一个启用开关，停用后到点不再执行，历史记录保留。
- **立即执行**：行内「立即运行」不等到点，马上执行一次；结果写入笔记时界面会提示可去笔记查看。
- **结果去哪了**：每次执行自动生成一条笔记，标题以「【定时任务】」开头（含任务名与执行时间），标签含「定时任务」，正文含提示词、执行专家与完整回复，在笔记页搜「定时任务」即可回看全部历史。
- **执行频率**：服务端每 20 秒检查一次到期任务；间隔任务最快 5 分钟一轮；上一次没结束的任务本轮自动跳过，不会并发跑两次。
- **模型未配置时**：到期任务标记为「skipped（跳过）」，不会真的发请求；配置好模型后，下一个周期自动恢复执行。

## 8. 数据安全与备份

- 所有数据保存在本机 `backend\data\workbench.db`，**重启电脑/应用都不会丢失**。
- 建议每周双击一次 **备份数据.bat**：采用在线热备份（服务运行中也可备份），文件按时间戳放在 `backups\`，自动只保留最近 30 份。
- 会话记录也在这个库里，删库等于删除全部数据，删前请先备份。

## 9. 常见问题

- **启动失败提示端口占用**：双击 `停止服务.bat` 停掉旧进程；或改端口（见《注意事项.md》第 1 条）。
- **AI 助手提示「模型未配置」**：按 6.1 配置供应商与模型并激活；只想安静用工作台可忽略。
- **不想要示例数据**：在 `backend/.env` 设置 `SEED_ON_STARTUP=0` 后重启，或执行 `venv\Scripts\python -m app.init_db --reset` 重置（见《注意事项.md》第 4 条）。
- **对话报错「助手服务暂时不可用」（502）**：多为模型渠道网络/鉴权问题，检查 Key 是否过期、base_url 与模型名是否匹配，或换一个模型再试。
