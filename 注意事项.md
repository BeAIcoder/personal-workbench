# 个人工作台 · 注意事项（避坑指南）

## 1. 端口占用

服务默认用 **8000** 端口。启动失败提示端口占用时：

- 双击 `停止服务.bat` 停掉旧进程后重试（脚本会校验占用进程确为 uvicorn/python 并请你确认后才结束，避免误杀其它程序）；
- 或改端口：把 `backend\.env.example` 复制为 `backend\.env`，设置 `PORT=8001`，并在 bat 里同步修改 uvicorn 的 `--port`。

开发模式还会用 **5173** 端口（Vite），冲突时改 `frontend/vite.config.js` 的 `server.port`。

## 2. Python / Node 环境

- 要求 Python 3.12+（实测 3.13 可用）、Node.js 18+，且都已加入 PATH。
- 若 `python` 命令不存在，检查安装时是否勾选了 "Add Python to PATH"。
- 首次启动依赖安装慢或失败时，脚本会自动切换到清华 pip 镜像 / npmmirror npm 镜像重试；仍失败请检查网络或代理。

## 3. 数据文件位置与备份

- 数据库：`backend\data\workbench.db`（单个 SQLite 文件，含任务/日程/笔记/AI 会话/专家团队/模型配置全部数据）。
- **不要**在服务运行时直接剪切/覆盖该文件；备份请用 `备份数据.bat`——它调用 `backend\scripts\backup_db.py` 做**在线热备份**（sqlite3 backup API，运行中也可安全执行），备份文件按时间戳放在 `backups\`，自动只保留最近 30 份。
- 迁移到新电脑：整个 `personal-workbench` 文件夹拷走（可不含 `backend/venv`、`frontend/node_modules`、`frontend/dist`），在新机上双击 `启动工作台.bat` 会自动重建环境，数据把 `workbench.db` 一起带走即可。

## 4. 重置 / 清空数据

在 `backend` 目录执行：

```bash
venv\Scripts\python -m app.init_db --reset
```

会清空全部数据并重新写入示例数据。只想要空库不写示例数据，在 `backend\.env` 设置 `SEED_ON_STARTUP=0` 后启动。
注意：专家团队定义（`agent_specs` 表）属配置数据，只在表为空时灌入一次，**不受 SEED_ON_STARTUP 控制**；想恢复内置专家出厂定义，用设置抽屉「专家团队」tab 的「恢复出厂」按钮。

## 5. 修改前端代码后需要重新构建

日常使用访问的是 `frontend\dist` 里的构建产物。改了前端代码后：

- 开发调试用 `开发模式.bat`（5173 端口，实时热更新）；
- 正式生效需在 `frontend` 目录执行 `npm run build`，然后重启服务。

## 6. 时间说明

- 系统使用本机本地时间（无时区换算），「今天」按电脑日期计算。
- 全天日程在数据库里存为 00:00:00–23:59:59。

## 7. 接口安全

- 服务只监听 127.0.0.1，仅本机可访问；没有登录鉴权，**不要**改为 0.0.0.0 暴露到局域网/公网（如需远程使用，请先加一层 VPN 或反向代理鉴权）。
- 所有查询走 SQLAlchemy 参数绑定，无 SQL 注入风险。
- 日期查询参数（如 `due_before`、`start`）格式非法会返回 422 并提示正确格式，不会报 500。

## 8. 其他

- 4 个 bat 脚本是 **GBK 编码**，用编辑器修改时注意保存为 ANSI/GBK，否则中文会乱码。
- 接口返回 422 表示提交的数据没通过校验（如标题为空、结束时间早于开始时间），页面会弹出具体原因。
- 测试使用独立临时数据库，不会碰真实数据，可放心运行 `pytest`。

## 9. AI 助手（Agent 团队）注意事项

- **模型密钥**：API Key 保存在本机（`model_providers` 表或 `backend\.env`），**以明文存储**——这是本机单人应用的设计取舍，请勿把数据库文件或 `.env` 外发（两者均已被 `.gitignore` 排除）；界面只回显脱敏形式。
- **未配置模型时的表现**：AI 助手页顶部显示「模型未配置」并给出指引；工作台其它功能完全不受影响。
- **对话报错 502**：多为模型渠道侧问题（Key 过期、模型名/base_url 不匹配、网络不通），换模型或核对配置后重试；流式对话则会在流内收到 error 事件，历史里留下一条错误消息。
- **专家团队改动即生效**：增删专家、改关键词、绑定模型保存后无需重启（缓存自动失效）；词太多太泛会降低路由准确度，可用「路由测试」验证。
- **「允许 Bash」与「插件」默认关闭**：开启后 Agent 可执行本机命令 / 加载本地插件代码（Bash 自带危险文件黑名单，只读模式下不加载插件），请知悉风险后再启用，且仅限本机使用场景。
- **LLM 直接写数据的边界**：Agent 只能新建任务/日程/笔记、完成任务，**不能物理删除任何数据**；删除请在页面上操作。每次工具调用都有轨迹可查。
- **费用**：Agent 对话按模型 token 计费；「深度研究」模式轮次更多、更耗 token，大批量粘贴报表分析时留意用量。
- **版本锁定**：`requirements.txt` 已锁定 `agentscope==2.0.7.post1`。该框架版本演进较快（0.x→1.0→2.0 有破坏性变更），升级前务必先跑 `pytest`。
- **依赖体积**：安装 agentscope 会带来约 150MB 依赖（含多家模型 SDK 与监控组件），首次启动 `启动工作台.bat` 会稍慢属正常。
- 架构与演进路线详见 [Agent团队设计.md](Agent团队设计.md)。
